<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.musicpan.mapper.MemberMapper">

	<resultMap type="com.musicpan.domain.MemberVO" id="memberMap">
		<id property="id" column="id"/>
		<result property="id" column="id"/>
		<result property="pwd" column="pwd"/>
		<result property="name" column="name"/>
		<result property="email" column="email"/>
		<result property="regDate" column="regdate"/>
		<result property="user_del" column="user_del"/>
		<result property="grade" column="grade"/>
		<collection property="authList" resultMap="authMap">
		</collection> 
	</resultMap>
	 
	<resultMap type="com.musicpan.domain.AuthVO" id="authMap">
		<result property="id" column="id"/>
		<result property="auth" column="auth"/>
	</resultMap>
  
	<select id="read" resultMap="memberMap">
		SELECT 
		  mem.id,  pwd, name, email, regdate, user_del, grade, auth
		FROM 
		  member mem LEFT OUTER JOIN member_auth auth on mem.id = auth.id 
		WHERE mem.id = #{id} 
	</select>
	
	
	<insert id="insert">
		insert into member(id, pwd, name, email, regdate, user_del, grade) values (#{id},#{pwd},#{name},#{email},now(),0,0)
	</insert>
	
	<insert id="insert_auth1">
		insert into member_auth(id, auth) values (#{id}, 'ROLE_USER')
	</insert>
	
</mapper>
